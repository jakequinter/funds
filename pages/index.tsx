import type { NextPage } from 'next';
import Head from 'next/head';

import Container from '@/components/Container';

const Home: NextPage = () => {
  return (
    <Container>
      <Head>
        <title>funds</title>
      </Head>
    </Container>
  );
};

export default Home;
